# Handoff prompt — FVG x Fractal-Corridor intraday study (TICK-035 / HYP-098)
# Paste everything below this line into a fresh Claude Code session.

You are running THE RESEARCH METHOD (below) in /Users/taboost/quant on a new intraday study. Work in a **git worktree** (another Claude session may be active — repo law after the June-29 incident). Before anything: read `NEXT.md` (top 5 entries), `research/yield_frontier/OPTIMIZATION_PROGRAM.md` (the method is codified there), `RISK_CONSTITUTION.md`, `TRADING_PHILOSOPHY.md`, and memory index `~/.claude/projects/-Users-taboost-quant/memory/MEMORY.md`. Multi-session law: before claiming any ticket/hypothesis number, re-read `tickets/backlog.md` tail and the ledger (`data/agent/hypothesis_ledger.json`) — TICK-035 and HYP-098 are reserved for you but verify they are still free; re-read file tails before any append.

**MISSION.** Test whether intraday Fair-Value-Gap structure, paired with the Fractal Corridors indicator, contains a tradable edge on NQ — and whether it beats the shop's current best sealed results. Benchmarks to beat, named now: constitutional yield > **0.023%/day** (HYP-093 fade at its sealed sizing) with holdout Sharpe reported against carry's **1.25** for context. Instrument: **NQ 1-min** (`data/es_nq/nq_globex_1min.parquet`, 2018-01→2026-06, ts_event UTC index). This is Route B (intraday = new data): the sealed daily nulls — HYP-082 fractal-beyond-carry, HYP-083 daily-FVG, ICT forex gates p=0.52, HYP-090 adaptive params — are settled; do NOT re-litigate daily resolution, and disclose these priors in every prereg.

**THE RESEARCH METHOD (follow exactly):**
1. Lookahead scan to find candidates — any data, any lookahead, inside the MINING WINDOW ONLY (**2018-01 → 2024-06**; access NQ through a loader that hard-truncates, copy `research/yield_frontier/holdout_guard.load_nq`). Target 50–100+ green configurations. Stamp every artifact `MINING — not evidence`; count every configuration in an append-only trials ledger (copy the `mined_n.json` pattern from `research/yield_frontier/_lib.py::record_mined`).
2. Find what survivors have in common — extract structure (killzone? displacement size? corridor-depth confluence? HTF alignment?), not parameters.
3. Pre-register before touching holdout — null, exact test, operational definitions, verdict rule, BOTH priors (Colin's and yours), hash-locked to `data/research/preregister/HYP-098_*.json` + ledger PREREGISTERED entry (backup ledger first, timestamped `.bak-`). Nothing moves after the hash.
4. Test on untouched data — holdout **2024-07 → 2026-06**, zero lookahead. Disclose that HYP-095 ran ONE daily-resolution test on this span (family accounting). Stats machinery already exists — use it: `research/modern/_lib.py` (block bootstrap L=5/10k/seed 42, `daily_sharpe`, `canonical_hash`), `sovereign/discovery/gate.py` (`deflated_sharpe_ratio` at n_trials = your FULL mined count — note it takes a NON-annualized Sharpe — and `benjamini_hochberg` if multiple hypotheses). Add per-year non-degrade and a locked tail condition. Constitutional sizing per RISK_CONSTITUTION Art. 1: 0.75% worst-case per trade with a DECLARED worst-case beyond the stop (see HYP-097's LULD-style reasoning; for NQ declare an overnight/limit scenario or intraday-only with a stop-gap model — pre-declare it, precedent in `data/research/preregister/HYP-095_yield_frontier.json` and `HYP-097_gapthrough_sizing.json`). NQ costs: `research/yield_frontier/frictions.py::NQ_RT_COST_PTS` (MNQ 0.87 pts/RT).
5. What survives is the innovation. Seal the verdict to the ledger (hash verified pre/post). A well-powered null is a valid, valuable outcome — file it with pride and move to the NEXT candidate family.

**"Don't stop until you find something" means:** iterate rounds of steps 1–5 over genuinely NEW candidate families (different structure, not re-tuned parameters) until either a survivor seals or you have exhausted the honest candidate space — with every round's trials added to the counter so the deflation penalty grows as you search. The bar NEVER lowers. Never re-test a sealed hypothesis on the same data. If you exhaust NQ families, STOP and write the map of where the edge isn't — that is a deliverable, not a failure. Do not switch instruments without Colin (intraday FX needs Dukascopy ingestion — a separate approved build; ThetaData is EOD-options-only Value tier, terminal DOWN, do not restart it).

**ASSETS (reuse, don't rebuild):** FVG detection: `sovereign/forex/ict_engine.py` (`_find_fvgs`, intraday-capable) and `ict/fvg_detector.py` — WARNING: the latter computes ATR over the whole passed frame (lookahead leak) — truncate the frame at decision time or recompute causally; write a test proving no post-t bar influences a signal at t (look-ahead canary precedent in `research/yield_frontier/tests/test_gates.py`). Killzones: `ict/session_classifier.py` (London 02-05, NY_Open 07-10, NY_PM 13:30-16 UTC). Fractal Corridors: the Pine v6 source is at repo root (`Fractal Corridors`) — port the multi-depth pivot-corridor logic to Python faithfully (pivots must use only CLOSED bars — Pine pivots confirm N bars late; model that delay explicitly or you leak). Python cousin for reference: `sovereign/sentiment/geometry_feed.py::corridor_stats` (daily; do not reuse blindly). Session arrays precedent: `research/yield_frontier/m2_nq.py::build_rth_arrays`.
**LAW (non-negotiable):** execution-path freeze (never touch `forex_exit_manager`, `decide_exit`, live scan, `ict/pipeline.py`); Article 6 — no model training, no live wiring, no OANDA writes; new code isolated under `research/fvg_corridor/` (imports whitelist: `research.modern._lib`, `research.yield_frontier.*`, `sovereign.discovery`, `sovereign.reporting` — enforce with an AST test, template in `research/yield_frontier/tests/test_gates.py`); outputs only under `data/research/fvg_corridor/`; module tests NOT collected by the main suite (`pytest.ini` testpaths) — the 40-known-failure baseline must be untouched; anything >10 minutes runs as chunked foreground (`--max-*` flags) — long background runs get externally killed here (documented 6+ times); commit per phase `[RESEARCH] TICK-035 ...`, push per batch; end every session with a NEXT.md entry (newest-first, re-read the top before inserting). If blocked or ambiguous: write the blocker to NEXT.md and stop — never silently improvise. Run `python3 -m pytest tests/ -k test_pipeline_does_not_import_sovereign -q` before every push.

Begin with Step 0: write the study charter (`research/fvg_corridor/CHARTER.md`) restating all of the above in your own words, then the holdout-fenced loader + its tests, then Step 1 mining. Good hunting — and remember the shop's culture: verify before you claim, a well-powered null is a paid-for map, and when a result is exciting, that is the moment to slow down.
