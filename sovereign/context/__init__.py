"""Read-only context assembly for trade time.

Nothing in this package is importable by, or imports from, the frozen execution
path. It reads the repo's research record and returns it as a packet a human or
an agent can consume before a trade is taken.
"""
