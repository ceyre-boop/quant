"""Positioning-family (HYP-072..081) research runners — READ-ONLY consumers of the
sentiment board tables and yfinance spot. Nothing here is importable by the live or
backtest execution path, and nothing here trains a model.

Discipline: every run re-verifies the pre-registration hash locks before touching
data (gate zero); results are sealed as dated ledger ANNOTATIONS — never verdicts —
until the family BH adjudication runs across all 10 primaries (options legs pending).
"""
