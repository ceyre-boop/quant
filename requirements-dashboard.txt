# Minimal deps for the hosted dashboard backend (scripts/live_signals_server.py).
# Deliberately NOT the full requirements.txt (no torch/xgboost/firebase) — keeps the
# Render build small and fast. Live market data via yfinance, chat via anthropic.
yfinance>=0.2.0
requests>=2.31.0
anthropic>=0.40.0
pandas>=2.0.0
numpy>=1.24.0
pytz>=2023.3
