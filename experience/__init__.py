"""experience/ — the memory organ: journal → attribution → weekly review → proposal.

Wisdom = lived experience, metabolized under law. The law is ATTRIBUTION_RUBRIC.md
(committed before the first classification); the journal is an OBSERVER — it derives
rows from the shadow log, decision logs, and proof-of-life snapshots, and never
instruments or imports the execution path. Abstention is a decision and is journaled
(RISK_CONSTITUTION Article 4, made observable).

The machine proposes (weekly review writes status=PROPOSED ledger entries); only the
operator promotes. Self-play under fixed rules: the loop improves the player, never
the rules.
"""
